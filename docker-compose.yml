services:
  classroom-downloader:
    build: .
    container_name: classroom-downloader
    volumes:
      - ./data:/data
      - ./downloads:/downloads
    environment:
      - SYNC_INTERVAL_MINUTES=${SYNC_INTERVAL_MINUTES:-60}
    restart: unless-stopped
